<table>
  <thead>
    <tr>
      <th>Article</th>
      <th>Notes</th>
      <th>Borrowed since</th>
      <th>Returned</th>
    </tr>
  </thead>
  <tbody>
    {{#each model as |loan|}}
      {{loans/loan-row
        loan=loan
        toggleReturnedAndSave=(action toggleReturnedAndSave)
      }}
    {{/each}}
  </tbody>
</table>
